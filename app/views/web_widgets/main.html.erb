<div class="container-fluid">
	<div class="row">
		<div class="col-xs-12 col-md-12">
			<% if @current_user %>
				<% unless @result.blank? %>
					
				<% else %>
					
				<% end %>	
			<% else %>
				<button type="button" class="btn btn-primary btn-lg btn-block">Login with MySize</button>
			<% end %>
		</div>
	</div>
</div>